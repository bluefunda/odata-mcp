name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  ci:
    uses: bluefunda/release-foundry/.github/workflows/go-ci.yml@main
    with:
      go-version: '1.25'
    secrets:
      GH_PAT: ${{ secrets.GH_PAT }}
